apiVersion: apps/v1
kind: Deployment
metadata:
  name: brain-service
  labels:
    app: brain-service
    part-of: agrisense
spec:
  replicas: 1
  selector:
    matchLabels:
      app: brain-service
  template:
    metadata:
      labels:
        app: brain-service
    spec:
      containers:
        - name: brain-service
          image: gcr.io/REPLACE_PROJECT_ID/agrisense/brain-service:latest
          ports:
            - containerPort: 3002
          envFrom:
            - secretRef:
                name: agrisense-secrets
            - configMapRef:
                name: agrisense-config
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          livenessProbe:
            httpGet:
              path: /health
              port: 3002
            initialDelaySeconds: 15
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 3002
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: brain-service
spec:
  selector:
    app: brain-service
  ports:
    - port: 3002
      targetPort: 3002
  type: ClusterIP
