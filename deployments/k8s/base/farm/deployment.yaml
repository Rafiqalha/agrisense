apiVersion: apps/v1
kind: Deployment
metadata:
  name: farm-service
  labels:
    app: farm-service
    part-of: agrisense
spec:
  replicas: 1
  selector:
    matchLabels:
      app: farm-service
  template:
    metadata:
      labels:
        app: farm-service
    spec:
      containers:
        - name: farm-service
          image: gcr.io/REPLACE_PROJECT_ID/agrisense/farm-service:latest
          ports:
            - containerPort: 3003
          envFrom:
            - secretRef:
                name: agrisense-secrets
            - configMapRef:
                name: agrisense-config
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          livenessProbe:
            httpGet:
              path: /health
              port: 3003
            initialDelaySeconds: 15
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 3003
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: farm-service
spec:
  selector:
    app: farm-service
  ports:
    - port: 3003
      targetPort: 3003
  type: ClusterIP
